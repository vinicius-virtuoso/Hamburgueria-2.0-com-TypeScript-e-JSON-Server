import { Box, Container } from "@chakra-ui/react";

import { Header } from "../components/Header";

export const Catalog = () => {
  return (
    <Box minH="100vh">
      <Header />
      <Container maxW="container.xl">ds</Container>
    </Box>
  );
};
